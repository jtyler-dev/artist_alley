import React from "react";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Account | Artist Alley",
};

export default function AccountPage() {
  return (
    <div>
      <h1>Account</h1>
    </div>
  );
}
